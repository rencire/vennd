'use strict';

export function getIntersections(circles) {
  
  // circle = {id: settings.id_cnt, point: point, radius: settings.radius, selected: false, fileContent: e.target.result};
  
  // 'circles' need to be an array
  if (!circles || circles.constructor !== Array) return null;

  var points = [];

  return points;
}
